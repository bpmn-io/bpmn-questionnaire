language: node_js
node_js:
  - "4.3.2"

script:
  - grunt test

before_install:
  - npm install -g grunt-cli
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

before_script:
  - grunt build-css
